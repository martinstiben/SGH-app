spring.application.name=SGH

# --- Configuración por Ambiente ---
# Los valores se sobreescriben por los archivos .env correspondientes

# --- Base de datos ---
spring.datasource.url=jdbc:mysql://localhost:3306/horarios
spring.datasource.username=user
spring.datasource.password=userpass
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# --- JPA / Hibernate ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.jdbc.batch_size=20
spring.jpa.properties.hibernate.order_inserts=true
spring.jpa.properties.hibernate.order_updates=true
spring.jpa.properties.hibernate.jdbc.batch_versioned_data=true

# --- Pool de Conexiones ---
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.maximum-pool-size=20
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.max-lifetime=1200000

# --- Usuario master (custom properties) ---
app.master.username=${MASTER_USER:master}
app.master.password=${MASTER_PASS:Master$2025!}

# --- JWT ---
jwt.secret=${JWT_SECRET:K9y!rQ2#Z8pLw@5xVmB3^nF1tH7dS0cG}
jwt.expiration=${JWT_EXPIRATION:3600000}

# --- Swagger/OpenAPI ---
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui/index.html
springdoc.swagger-ui.tags-sorter=alpha
springdoc.swagger-ui.operations-sorter=alpha

# --- Puerto ---
server.port=${PORT:8082}

# --- CORS Configuration ---
spring.web.resources.cors.allowed-origins=${CORS_ORIGINS:http://localhost:3000,http://localhost:8080}
spring.web.resources.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS,PATCH
spring.web.resources.cors.allowed-headers=*
spring.web.resources.cors.allow-credentials=true

# --- Email Configuration ---
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:msnarvaez06@gmail.com}
spring.mail.password=${MAIL_PASSWORD:ekqk iyzb tkqa durr}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# --- File Upload ---
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB

# --- Actuator (Health Checks) ---
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=always

# --- Logging ---
logging.level.com.horarios.SGH=INFO
logging.level.org.springframework.security=DEBUG